#!/usr/bin/env node

var regenerate = require('..'),
    options = require('../lib/options.js'),
    argv;

argv = options.parse();

if (argv.file == null || argv.help) {
    options.usage();
} else {
    regenerate(argv);
}
